#!/bin/bash

if [ "$#" -ne 2 ]; then
  echo "Usage:"
  echo ""
  echo "  git mirror SOURCE TARGET"
  echo ""
  exit -1
fi

set -e
set -u

SOURCE=$1
TARGET=$2

git fetch --prune "${SOURCE}"
git push --prune $TARGET refs/remotes/${SOURCE}/*:refs/heads/*
